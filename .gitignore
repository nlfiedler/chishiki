# Rust build artifacts
/target/

# Local server data (blob store + SQLite metadata); see CHISHIKI_DATA
/data/

# Backwards-compat: crate-local target dirs (should not appear with a workspace)
**/target/

# macOS
.DS_Store
